import React from "react";

function Options({ textInput }) {
  return (
    <div className="w-full text-wrap text-gray-700 bg-slate-300 text-center rounded-md p-2">
      {textInput}
    </div>
  );
}

export default Options;
